---
import type { CollectionEntry } from "astro:content";
import { ArrowUpRight } from "@lucide/astro";

interface Props {
  project: CollectionEntry<"projects">;
  index: number;
}

const { project, index } = Astro.props;
const { data } = project;
const filters = [data.company, ...data.areas].join("|").toLowerCase();
---

<article class="project-entry" data-project data-filters={filters}>
  <p class="project-index">{String(index + 1).padStart(2, "0")}</p>
  <div class="project-entry-main">
    <p class="project-context">{data.company} · {data.areas.slice(0, 2).join(" · ")}</p>
    <h2><a href={`/case-studies/${project.id}/`}>{data.title}</a></h2>
    <p class="project-description">{data.description}</p>
    <ul class="tag-list" aria-label="Technology stack">
      {data.techStack.slice(0, 6).map((tech) => <li>{tech}</li>)}
    </ul>
  </div>
  <div class="project-entry-aside">
    {data.metrics[0] && (
      <p class="entry-metric"><strong>{data.metrics[0].value}</strong><span>{data.metrics[0].label}</span></p>
    )}
    <a href={`/case-studies/${project.id}/`} aria-label={`Read ${data.title} case study`}>
      View case study <ArrowUpRight size={15} aria-hidden="true" />
    </a>
  </div>
</article>
