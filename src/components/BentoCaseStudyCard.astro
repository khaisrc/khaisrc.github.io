---
import type { CollectionEntry } from "astro:content";
import { ArrowUpRight } from "@lucide/astro";

interface Props {
  caseStudy: CollectionEntry<"caseStudies">;
  variant: "lead" | "wide";
  area: string;
}

const { caseStudy, variant, area } = Astro.props;
const { data } = caseStudy;
const metric = data.metrics[0];
---

<article class:list={["bento-tile", "bento-case-study", `bento-case-study-${variant}`]} style={`--bento-area: ${area}`}>
  <div>
    <p class="tile-kicker">{data.company} · {data.areas[0]}</p>
    <h2><a href={`/case-studies/${caseStudy.id}/`}>{data.title}</a></h2>
    <p class="tile-description">{data.description}</p>
  </div>
  <div class="tile-footer">
    {metric && (
      <p class="tile-metric"><strong>{metric.value}</strong><span>{metric.label}</span></p>
    )}
    <a class="tile-link" href={`/case-studies/${caseStudy.id}/`} aria-label={`Read ${data.title} case study`}>
      Read case study <ArrowUpRight size={15} aria-hidden="true" />
    </a>
  </div>
</article>
